<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<h1>ユーザ 一覧</h1>
<%= link_to('新規作成', new_user_path) %>

<% @users.each do |user| %>
  <p>
    名前: <%= user.name %>
    Eメール: <%= user.email %>
    <%= link_to('詳細', user_path(user.id)) %>
  </p>
<% end %>
