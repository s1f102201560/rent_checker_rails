<%= render 'success'  %>
<%= render 'error'  %>

<h1><%= t 'users.show.title', user_name: @user.name  %></h1>
<p>
  名前: <%= @user.name %>
  Eメール: <%= @user.email %>
  <%= link_to '編集',  edit_user_path(@user) %>
  <%= link_to '削除', user_path(@user), data: {turbo_method: 'delete', turbo_confirm: '本当に削除しますか？'} %>
</p>

<%= link_to '戻る', users_path %>
